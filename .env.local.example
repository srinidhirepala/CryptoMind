# Environment Variables for CryptoMind
# Copy this file to .env.local and fill in your actual API keys

# === REQUIRED ===

# OpenAI API Key (GPT-4o-mini for AI features)
#Openrouter key 
# Get it at: https://platform.openai.com/api-keys
OPENAI_API_KEY=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

# === OPTIONAL (Recommended for production) ===

# Etherscan API Key (for transaction history — free tier works but is rate-limited)
# Get it at: https://etherscan.io/apis
ETHERSCAN_API_KEY=your-etherscan-api-key-here

# Next.js public URL (used for generating absolute URLs in production)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# === NOTES ===
# - CoinGecko API: No key needed for free tier (60 calls/minute)
# - MetaMask: Installed in browser by user, no server key needed
# - All API keys are server-side only (no NEXT_PUBLIC_ prefix) for security
